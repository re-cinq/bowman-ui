# .gitignore is honoured automatically (Prettier 3), so dist/, coverage/ and
# node_modules/ need no entry here.

# The test-command manifest in this directory is hand-maintained in this
# repository and formatted by hand. The directory stays out of the formatter
# because an external tool reads it: that tool's onboarding draft was not even
# valid YAML (a markdown-fenced document in a .yml file), which fails
# `prettier --check` outright rather than merely reporting a formatting
# difference, and any draft it writes here again must not red the check.
.lore/
