export function firstImpl(input: number) {
  const doubled = input * 2;
  const shifted = doubled + 1;
  return shifted;
}

export function secondImpl(input: number) {
  const doubled = input * 2;
  const shifted = doubled + 1;
  return shifted;
}

export function thirdImpl(input: number) {
  const doubled = input * 2;
  const shifted = doubled + 1;
  return shifted;
}
